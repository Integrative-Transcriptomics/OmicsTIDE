
const DiagramId = Object.freeze({profile: "profile", centroid: "centroid", box: "box"});

const TabId = Object.freeze({matrix: "matrix", intersecting: "intersecting", nonIntersecting: "nonIntersecting", selectionIntersecting: "selectionIntersecting", selectionNonIntersecting: "selectionNonIntersecting"});

const AnalysisTypeId = Object.freeze({overview: "overview", selection: "selection"});


// function fileComparisonIdFromTabId(tabId){
// 	return tabId.split("-")[0];
// }

// function comparisonTypeIdFromTabId(tabId){
// 	return tabId.split("-")[1];
// }

// function analysisTypeIdFromTabId(tabId){
// 	return tabId.split("-")[2];
// }

// function tabCountFromTabId(tabId){
// 	return tabId.split("-")[3];
// }

// function diagramIdFromTabId(tabId){
// 	return tabId.split("-")[4];
// }

// function datasetIdFromTabId(tabId){
// 	return tabId.split("-")[5];
// }

// function clusterIdFromTabId(tabId){
// 	return tabId.split("-")[6];
// }

// function datasetClusterFromTabId(tabId){
// 	return tabId.split("-")[5] + "_" + tabId.split("-")[6];
// }

